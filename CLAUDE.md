## OpenX-S engineering guide

This is OpenX-S v3.0.0 — Stellar-native AI assistant marketplace. The single
source of truth for context is `docs/PROJECT_CONTEXT.md`. PRD lives in
`docs/prd/PRD-S-stellar-native-mvp.md`. Ops in `docs/runbooks/STELLAR_DEPLOY.md`.

Smoke gate: `npm run smoke:all`. Build verification: `npm install && npm run build && cargo build --manifest-path packages/soroban-contracts/Cargo.toml`.

